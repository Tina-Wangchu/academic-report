# 报告格式设计 / Report Format Design

> 本文档定义 Agent Scholar 生成的学术报告的完整格式规范，供 `report_generator.py`（模块5）及报告模板（`templates/report_template.md`、`templates/report_html_template.html`）实现时遵循。所有内容以中英双语呈现。
>
> This document defines the complete format specification for academic reports generated by Agent Scholar. It guides the implementation of `report_generator.py` (Module 5) and the report templates (`templates/report_template.md`, `templates/report_html_template.html`). All content is presented bilingually in Chinese and English.

---

## 1. 设计目标 / Design Goals

- **可速读 / Scannable**：用户在 30 秒内即可了解报告覆盖了哪些研究主题。
  Users can grasp which research topics the report covers within 30 seconds.
- **按主题聚类 / Topic-clustered**：将相似或相关研究方向的论文归为同一“热点”，便于横向对比。
  Papers with similar or related research directions are grouped into the same "hotspot" for easy cross-comparison.
- **可追溯 / Traceable**：每篇论文标注作者、发表时间、期刊、引用量、DOI，并提供该方向奠基性参考论文。
  Each paper is annotated with authors, publication time, venue, citation count, and DOI; foundational reference papers are provided for each direction.
- **前瞻性 / Forward-looking**：报告末尾给出该领域的未来研究趋势与研究缺口。
  The report ends with future research trends and research gaps for the field.

---

## 2. 报告整体结构 / Overall Report Structure

报告由三个主要部分组成，顺序固定：

The report consists of three main parts in a fixed order:

| 序号 / No. | 章节 / Section | 内容 / Content |
|---|---|---|
| — | 标题 + 时间 / Title + Time | 时间范围 + 领域/主题 + 报告；生成时间与涵盖时间（小字）<br>Time range + field/topic + Report; generation time and coverage time (small font) |
| 一 / I | 报告速览 / Report Overview | 按热点分组、逐篇概述每篇论文核心内容<br>Grouped by hotspot, summarizing each paper's core content |
| 二 / II | 分类论文展示 / Classified Paper Display | 按“热点”聚类展示论文，每类含介绍、论文、整体分析与奠基性参考<br>Papers grouped by "hotspot"; each group has an intro, papers, an overall analysis, and foundational references |
| 三 / III | 研究趋势 / Research Trends | 约 200 字的未来趋势与研究缺口分析<br>~200-word analysis of future trends and research gaps |

---

## 3. 标题与时间 / Title and Time

### 标题 / Title

**格式 / Format**：`{时间范围} {领域/主题} 报告`（中文或英文）

**示例 / Example**：
- 中文：`2023-2025 统计学研究报告`
- English: `2023-2025 Statistics Research Report`

> 时间范围取自搜索意图中的 `start_date`–`end_date`；领域/主题取自 `intent.research_field` 或 `intent.query`。
>
> The time range comes from `start_date`–`end_date` in the search intent; the field/topic comes from `intent.research_field` or `intent.query`.

### 时间（小字）/ Time (small font)

标题下方以小字列出两个时间 / Below the title, list two timestamps in a small font:

- **报告生成时间 / Report generation time**：报告被生成的时刻（如 `2026-07-11 14:30`）。
  The moment the report is generated (e.g., `2026-07-11 14:30`).
- **报告涵盖时间 / Report coverage time**：本报告所收录论文的发表时间区间（如 `2023-01 至 2025-12`）。
  The publication time range of the papers included in this report (e.g., `2023-01 to 2025-12`).

---

## 4. 第一部分：报告速览 / Part I: Report Overview

- 篇幅 / Length：随热点数与论文数适度增长 / scales with hotspot and paper count.
- 写法 / Approach：**按热点分组、逐篇概述**——先点出本报告有哪些热点，再在每个热点下**逐篇概括其中每一篇论文的核心内容**（取该篇摘要前 1-2 句，即问题+方法），让用户一眼看到报告覆盖的全部论文及其要点。
  **Grouped by hotspot, per-paper**: state which hotspots the report covers, then under each hotspot summarize **every paper's core content** (first 1-2 sentences of its abstract, i.e. problem + method), so users see all papers and their points at a glance.
- 要求 / Requirement：覆盖全部热点与**每一篇论文**（不得遗漏）；每个热点给出热点名 + 篇数，其下列出「论文标题：核心内容」。
  Cover all hotspots and **every paper** without omission; each hotspot gives its name + paper count, followed by `Paper title: core content` lines.

> 示例 / Example：
> “本报告收录 12 篇论文，分为 3 个研究热点。以下按热点逐篇概述其核心内容：
> - 「扩散模型采样加速」(4 篇)：
>   - 《DDIM-Solver》：针对扩散模型采样步数过多的问题，提出一种新型高阶 ODE 求解器，将采样压缩至 10 步。
>   - 《Consistency-Model》：提出一致性模型实现一步高质量生成……
> - 「多模态对齐」(4 篇)：……”
>
> "This report includes 12 papers across 3 research hotspots. Each hotspot and its papers are summarized below:
> - Sampling Acceleration for Diffusion Models (4 papers):
>   - DDIM-Solver: Proposes a novel high-order ODE solver that compresses sampling to 10 steps.
>   - Consistency-Model: Introduces a consistency model for one-step high-quality generation...
> - Multimodal Alignment (4 papers): ..."

---

## 5. 第二部分：分类论文展示 / Part II: Classified Paper Display

本部分将相似研究方向的论文或相关的研究放在一起陈列。每一个类别称为一个“热点”。

This section groups together papers with similar research directions or related studies. Each category is called a "hotspot".

### 5.1 类别格式 / Category Format

每一个类别的格式如下（以热点一为例）/ The format for each category is as follows (taking Hotspot 1 as an example):

```
热点一：XXXX                ← 热点名称 / Hotspot name
热点主题的介绍：……          ← 该热点研究方向的简介 / Brief intro of this research direction

  论文一：标题               ← 论文标题 / Paper title
  作者、发表时间、发表期刊、引用量、DOI
  四要素摘录：解决的问题 / 现有方案 / 新方案 / 效果及局限性
  （从摘要中摘录对应语段；四要素全空时回退完整 Abstract）

  论文二：……

（该热点的整体分析）          ← 综合本热点几篇论文做的方向性整体分析 / Overall analysis
（奠基性参考论文）            ← 该方向上过往最具奠基意义、突破性的论文，作为参考 / Foundational references
```

### 5.2 单篇论文字段 / Per-Paper Fields

每篇论文必须包含以下字段 / Each paper must include the following fields:

| 字段 / Field | 说明 / Description |
|---|---|
| 标题 / Title | 论文完整标题 / Full paper title |
| 作者 / Authors | 作者列表（多于 3 位时用“等 / et al.”） / Author list (use "et al." when >3) |
| 发表时间 / Publication time | 发表年份或年月 / Publication year or year-month |
| 发表期刊 / Venue | 期刊或会议名称 / Journal or conference name |
| 引用量 / Citation count | 被引次数 / Number of citations |
| DOI | 数字对象标识符（若有） / Digital Object Identifier (if available) |
| 四要素摘录 / Four-element excerpts | **从摘要中摘录**四类语段（见下）；四要素全空时回退完整 Abstract / **Excerpted from the abstract** into four categories (below); falls back to full Abstract when all four are empty |

> **四要素 / Four elements（2026-07-13 改，替代原 Abstract 段）**：单篇论文块按以下四段展示，每段为从论文摘要（首选语段）中摘录的匹配语段：
>
> 1. **解决的问题 / Problem** — 论文针对的挑战/痛点（如「X 计算昂贵」「Y 存在 Z 缺陷」）；无显式问题句时回退首句（背景）。
> 2. **现有方案 / Existing approaches** — 先前/已有方法及其不足（引用先前工作的语段，如「prior solvers suffer from…」）。
> 3. **新方案 / New approach** — 本文提出的方法（如「we propose…」）。
> 4. **效果及局限性 / Results & limitations** — 实验结果 + 局限（如「outperforms…」+「struggles with…」）。
>
> 任一要素摘录不到时显示占位「未明确提及 / Not explicitly mentioned」；四要素全空（如摘要过短/无结构信号）时整段回退为完整 Abstract（缺失则「暂无摘要」）。摘录为规则版抽取式（按句匹配信号词，中英文均支持），不编造。
>
> **Four elements (2026-07-13, replacing the Abstract block)**: the per-paper block shows four excerpted segments from the paper's abstract:
> 1. **Problem** — the challenge/pain-point addressed; falls back to the first (background) sentence if no explicit problem sentence.
> 2. **Existing approaches** — prior/existing methods and their shortcomings (sentences citing prior work).
> 3. **New approach** — the proposed method ("we propose…").
> 4. **Results & limitations** — experimental results plus limitations.
> A missing element shows the placeholder「未明确提及 / Not explicitly mentioned」; if all four are empty, the block falls back to the full Abstract (or「暂无摘要」if absent). Extraction is rule-based extractive (sentence-level marker matching, CN/EN); it never fabricates.

### 5.3 类别整体分析 / Category Overall Analysis

在列出该热点的所有论文之后，综合这几篇论文做这个方向的**整体分析**：

After listing all papers in the hotspot, synthesize them into an **overall analysis** of this direction:

- 指出共同主题、方法上的演进与分歧；
  Identify common themes, methodological evolution, and divergences;
- 对比各论文的贡献与局限；
  Compare each paper's contributions and limitations;
- 点出该热点目前的发展阶段。

  Point out the current development stage of this hotspot.

### 5.4 奠基性参考论文 / Foundational Reference Papers

为该方向提供过往**最具奠基意义、突破性**的论文作为参考（通常 1-3 篇）：

Provide the most **foundational and groundbreaking** past papers for this direction as references (typically 1-3):

- 优先选择高被引、被本热点论文广泛引用的经典工作；
  Prioritize highly-cited classic works widely cited by the hotspot's papers;
- 每篇给出标题、作者、年份与简短说明（为何奠基）。
  For each, give title, authors, year, and a brief note on why it is foundational.

---

## 6. 第三部分：研究趋势 / Part III: Research Trends

- 篇幅 / Length：**约 200 字 / about 200 words**。
- 内容 / Content：结合本次报告的**所有论文**，做该研究领域的**未来研究趋势**与**研究缺口**分析。
  Combining **all papers** in this report, analyze the **future research trends** and **research gaps** of this research field.
- 要求 / Requirement：趋势与缺口都应从本次收录的论文中找到依据，避免空泛套话。
  Both trends and gaps should be grounded in the included papers; avoid generic platitudes.

---

## 7. 完整示例 / Full Example

```markdown
# 2023-2025 扩散模型研究报告
# 2023-2025 Diffusion Models Research Report

*报告生成时间 / Report generation time: 2026-07-11 14:30*
*报告涵盖时间 / Report coverage time: 2023-01 至 / to 2025-12*

## 一、报告速览 / I. Report Overview

本报告收录 6 篇论文，分为 2 个热点。以下按热点逐篇概述其核心内容：
This report includes 6 papers across 2 hotspots. Each hotspot and its papers are summarized below:

- 「扩散模型的采样加速 / Sampling Acceleration」(3 篇 / 3 papers):
  - 《DDIM-Solver》: 针对扩散模型采样步数过多的问题，提出一种新型 ODE 求解器，将采样压缩至 10 步。
  - 《Consistency-Model》: 提出一致性模型，实现一步高质量生成……
  - 《FastDPM》: ……
- 「可控生成 / Controllable Generation」(3 篇 / 3 papers):
  - 《ControlNet-X》: ……
  - 《DreamBooth-Lite》: ……

## 二、分类论文展示 / II. Classified Paper Display

### 热点一：扩散模型的采样加速 / Hotspot 1: Sampling Acceleration for Diffusion Models
热点主题的介绍：降低扩散模型推理步数是落地的关键瓶颈……
*Intro: Reducing inference steps is a key bottleneck for deploying diffusion models...*

**论文一 / Paper 1: DDIM-Solver: 10-Step High-Quality Sampling**
- 作者 / Authors: Zhang, L., Wang, Q., et al.
- 发表时间 / Published: 2024
- 发表期刊 / Venue: NeurIPS 2024
- 引用量 / Citations: 187
- DOI: 10.1234/neurips.2024.ddim-solver
- Abstract: 本文针对扩散模型采样步数过多、推理缓慢的问题，提出一种新型高阶 ODE 求解器；通过定制轨迹与高阶校正项保持样本质量，在 CIFAR-10 与 ImageNet 上仅需 10 步即可生成高质量样本，优于已有求解器……（200-300 字）

**论文二 / Paper 2: …**

**整体分析 / Overall Analysis**: 两篇工作分别从求解器设计与一致性蒸馏切入……
**奠基性参考论文 / Foundational References**:
- Song et al., 2021, *DDPM* —— 奠定扩散模型概率转移框架；
- Song et al., 2021, *Score-SDE* —— 建立 ODE/SDE 统一视角。

### 热点二：…… / Hotspot 2: ...

## 三、研究趋势 / III. Research Trends

综合本报告 6 篇论文，扩散模型研究正从“提升样本质量”转向“提升推理效率与可控性”。
未来趋势包括……研究缺口在于少步采样的理论保证仍不足……（约 200 字）
```

---

## 8. 格式规范总结 / Format Specification Summary

| 规范项 / Spec | 要求 / Requirement |
|---|---|
| 标题 / Title | 时间范围 + 领域/主题 + 报告（中/英）<br>Time range + field/topic + Report (CN/EN) |
| 时间 / Time | 生成时间 + 涵盖时间（小字）<br>Generation time + coverage time (small font) |
| 报告速览 / Overview | 按热点分组、逐篇概述每篇核心内容<br>Grouped by hotspot, per-paper core content |
| 分类展示 / Display | 按“热点”聚类，每类含介绍/论文/整体分析/奠基性参考<br>Grouped by "hotspot"; each has intro/papers/analysis/references |
| 单篇块 / Per-paper block | 基本信息 + 四要素摘录（解决的问题/现有方案/新方案/效果及局限）+ APA<br>Basic info + four-element excerpts + APA |
| 四要素摘录 / Four-element excerpts | 从摘要摘录对应语段；全空回退完整 Abstract<br>Excerpted from the abstract; falls back to full Abstract when all empty |
| 研究趋势 / Trends | 约 200 字，未来趋势 + 研究缺口<br>~200 words, future trends + research gaps |

---

## 9. 与实现的映射 / Mapping to Implementation

| 设计要求 / Design Requirement | 实现位置 / Implementation Location |
|---|---|
| 标题含时间范围与领域 / Title with time range + field | `report_generator.py::_render_markdown_report` 标题段 / title block |
| 两个时间戳 / Two timestamps | `intent.start_date`/`end_date` → 涵盖时间；`datetime.now()` → 生成时间 |
| 按热点逐篇速览 / Hotspot-grouped per-paper overview | `report_generator.py::_generate_summary`（按热点分组 + `_paper_finding` 逐篇概述） / grouped by hotspot, per-paper finding |
| 热点聚类 + 介绍 / Hotspot grouping + intro | `paper_filter.py::classify_by_topic` + 新增类别介绍生成 / add category-intro generation |
| 四要素摘录（问题/现有方案/新方案/效果及局限） | `paper_analyzer.StructuredExtractor`（从摘要按句匹配信号词摘录）+ `report_generator._render_paper`（四段渲染，全空回退完整 Abstract） / four-element excerpts |
| 整体分析 + 奠基性参考 / Overall analysis + foundational refs | `paper_analyzer.py` 新增方向级分析 + 奠基论文查找 / add direction-level analysis + foundational lookup |
| 约 200 字研究趋势 / ~200-word trends | `report_generator.py::_analyze_trends`（限字数） / cap to ~200 words |

---

## 10. 数据质量与降级规则 / Data Quality & Degradation Rules

> 端到端实验（`test/experiments/`，12 场景）暴露的真实数据质量问题及统一处理规则。实现须遵循。
>
> Real data-quality issues surfaced by end-to-end experiments, and the uniform handling rules. Implementations must conform.

### 10.1 Abstract 缺失与长度 / Missing Abstract & Length

- 部分数据源（尤其 OpenAlex）对某些论文不返回摘要。报告不得留空白段。
  Some sources (notably OpenAlex) return no abstract for some papers. The report must not leave a blank section.
- **规则 / Rule**：Abstract 为空时，显示占位「（暂无摘要 / No abstract available）」；速览中该篇内容回退为标题。
  When Abstract is empty, show the placeholder「（暂无摘要 / No abstract available）」; in the overview, that paper's content falls back to its title.
- **四要素摘录 / Four-element excerpts（2026-07-13 改，替代单篇 Abstract 段）**：单篇块按「解决的问题 / 现有方案 / 新方案 / 效果及局限性」四段展示，每段为从论文摘要中摘录的匹配语段（`paper_analyzer.StructuredExtractor` 按句匹配信号词；中英文均支持）。摘录不到的要素显示「未明确提及 / Not explicitly mentioned」；**四要素全空**（如摘要过短/无结构信号）时整段回退为完整 Abstract（`condensed_abstract`，去填充、≤1500 字符、无 `...`），仍无则占位「暂无摘要」。
  The per-paper block shows four excerpted segments — Problem / Existing approaches / New approach / Results & limitations — each excerpted from the abstract by sentence-level marker matching (`StructuredExtractor`; CN/EN). A missing element shows「未明确提及 / Not explicitly mentioned」; if **all four are empty**, the block falls back to the full de-filled Abstract (`condensed_abstract`, ≤1500 chars, no `...`), or the placeholder when absent.
- **不再单列分析字段 / No separate analysis fields**：单篇块**不再渲染**「研究内容 / 创新点 / 核心结论」——其内容已并入上述四要素摘录（新方案/效果及局限等），避免重复。
  The per-paper block **no longer renders** "Research content / Innovations / Conclusions" — their content is folded into the four-element excerpts above to avoid duplication.

### 10.2 创新点字段 / Innovations Field（已并入四要素，2026-07-13 起单篇块不再单列）

> **状态**：单篇块已改为四要素摘录（§11），「创新点」不再作为独立字段渲染——其语义并入「新方案 / 效果及局限」。下方规则保留供分析层内部计算参考。

- 信号词命中时给出双语标签（如「提出新颖方法 / proposes a novel method」）。
  When signal words match, give bilingual labels.
- **无信号词时不得给空泛默认值**（如「提出新的研究方法」）：改为**基于方法子句派生**（如「提出：{方法子句} / proposes: {method clause}」），让创新点落在该论文真实方法上。
  When no signal word matches, do **not** give a generic default; instead **derive from the method clause** so the innovation is grounded in the paper's real method.
- 无摘要时创新点留空（不渲染该字段）。
  When there is no abstract, leave innovations empty (the field is not rendered).

### 10.3 热点聚类相关性与收敛 / Hotspot Relevance & Consolidation

- 热点须与**所搜索主题相关**，而非单纯关键词匹配产生的噪声。
  Hotspots must be **relevant to the searched topic**, not noise from blind keyword matching.
- **规则 / Rule**：
  1. 兜底关键词需 **≥2 篇论文共享**才单列为热点；**单篇**的关键词桶并入「其他 / Miscellaneous」。
     A fallback keyword must be **shared by ≥2 papers** to form its own hotspot; **single-paper** keyword buckets merge into「其他 / Miscellaneous」.
  2. 同样地，已知主题词典命中但仅 1 篇的桶（常为误分类，如天文学论文被归入「计算机视觉」）也并入「其他」。
     Likewise, a known-topic bucket with only 1 paper (often a misclassification) merges into「其他」.
  3. 关键词抽取过滤通用学术虚词（approach/application/study/method/results/analysis 等），并在多候选时**优先选取与查询/领域相关的关键词**。
     Keyword extraction filters generic academic filler words and, among candidates, **prefers keywords relevant to the query/field**.
- **效果 / Effect**：噪声单论文热点（如 `Apple(1)`、`Clinical(1)`）消失，热点数从 ~17 收敛到 ~3-7 个真实方向。

---

## 11. 四要素摘录：演进过程与最终实现方法 / Four-Element Extraction: Evolution & Final Method

> 本节记录单篇块「四要素摘录」（解决的问题 / 已有方案 / 新方案 / 效果及局限）从设计到落地的**修改过程**与**最终实现方法**，供后续维护与升级（如接入 LLM）参考。实现位于 `paper_analyzer.py::StructuredExtractor`，渲染位于 `report_generator.py::_render_paper`。
>
> This section records the **evolution** and **final implementation** of the per-paper four-element excerpts (Problem / Existing approaches / New approach / Results & limitations).

### 11.1 为什么用四要素摘录（取代单段 Abstract）

- 用户反馈：单段 Abstract（即便 200-300 字）「概括过于草率，无法有效涵盖学术研究成果」。
- 改为按学术阅读直觉**结构化摘录**四类语段，从论文摘要（首选语段）中直接摘录匹配句，避免改写失真，也便于快速定位「这篇做了什么新东西」。

### 11.2 字段定义（关键澄清 / Key Definitions）

> 这是后续匹配规则的**语义基准**，必须先明确：

| 字段 | 含义 | 不是什么 |
|------|------|----------|
| **解决的问题 / Problem** | 论文针对的挑战/痛点/动机 | 不是背景介绍 |
| **已有方案 / Existing approaches** | **已经存在的理论、方法、思路**（既有范式、先前工作、被广泛采用的做法） | 不是「本文结果的对比基线」 |
| **新方案 / New approach** | **本文提出的创新内容**（不一定是「做一件事的方式」——可是新理论/发现/洞见/框架/模型/贡献） | 不只是 "we propose a method" |
| **效果及局限性 / Results & limitations** | 实验结果 + 局限 | 不是创新点本身 |

> 注：「已有方案 = 已经存在的理论/方法/思路」「新方案 = 本文创新内容（不限于方法）」是用户明确的定义，直接决定了下方信号词的选取。

### 11.3 演进过程（三版迭代 / Three Iterations）

#### v1（初版）—— 标记过窄，字段普遍缺失
- 「已有方案」只认 `prior / existing / conventional` 等**显式引用词**；「新方案」只认 `we propose/introduce a method`。
- **问题**：真实摘要里多数论文不这么写 → **existing / new 普遍缺失**（用户反馈）。
- 规则：`新 → 现有 → 问题 → 效果`，问题回退首句。

#### v2（拓宽定义）—— 字段不再缺失，但出现错位
- 按用户澄清拓宽：
  - 「新方案」加入发现/洞见/理论词（`we show/find/prove`、`our insight/contribution`、`novel/first to`、中文「发现/证明/揭示/首次」）。
  - 「已有方案」加入既有实践词（`widely used/adopted`、`has been studied/used`、`rely on`）+ **中性背景回退**（无显式标记时取首句中性背景）。
- 优先级调为 `新 → 现有(+背景回退) → 问题 → 效果`。
- **问题**：在结构混乱的摘要上「新方案 ↔ 效果」**互换**——例如背景句 `this work plays a vital role` 被当新方案；被动 `a solution is proposed`（本是新方案）因同句 `experimentally validated` 命中结果词被归到效果。

#### v3（当前版）—— 强/弱打分 + 结果对比剔除
- **「新方案」标记分强/弱并打分**：
  - 强信号（贡献动词 `we propose/show/find/prove`、新颖性 `novel/first to`、被动 `is proposed`、中文「提出/发现/证明/揭示/首次」）= +2；
  - 弱信号（`this work/paper/study`、`our model/method`）= +1；
  - 含既有工作标记 = −2（`methods have been proposed` 不算本文创新）。
  - → 取最高分句，**背景弱信号句不再抢走真正贡献句**。
- **「已有方案」打分择优**：须含既有标记；但**结果/对比句降权**（`compared to existing` / `outperforms` / 含本文创新词 → −1），避免把「本文 vs 既有」的结果对比当成既有方案。
- 修复回归 bug：`baselines` 当既有标记会误抢结果句（`outperforms baselines`），已从既有标记移除。
- **效果**：Apple 论文等混乱摘要上，「新方案↔效果」归位；existing 在无真正既有方案句时诚实留空或取领域背景。

### 11.4 最终实现方法 / Final Implementation

**入口**：`PaperAnalyzer._analyze_single_paper` 调 `StructuredExtractor().extract(paper)`，填入 `paper.problem / existing_approaches / new_approach / results_limitations`。

**算法（按优先级依次择句，互不重复，共用 `used` 集合）**：

```
sentences = 按句切分摘要（保护小数与缩写）
1. 新方案   = _pick_new()       打分：强+2 / 弱+1 / 既有-2，取最高分(>0)
2. 已有方案 = _pick_existing()  打分：须含既有标记+1，结果/对比/本文创新词各-1，取最高分(>0)
                               无 → _pick_background()：首句中性背景（无 new/problem/result/limitation 标记）
3. 解决的问题 = _pick(PROBLEM_MARKERS)   首个问题信号句
4. 效果及局限 = _pick_results()          1 结果句 + 1 局限句（拼接）
```

**信号词集合**（中英文均支持，节选）：
- `PROBLEM_MARKERS`：challenge / expensive / suffer / lack / obstacle / inefficient / slow / 问题 / 挑战 / 瓶颈 …（弱词 however/important、局限词 fails/struggle/limitation **不入**，避免与其它字段冲突）
- `EXISTING_MARKERS`：prior / existing / conventional / widely used / has been studied / rely on / 已有 / 现有 / 传统 …（`baseline` 已移除）
- `NEW_STRONG`：we propose / we show / we find / we prove / is proposed / novel / first to / 提出 / 发现 / 证明 / 揭示 / 首次 …
- `NEW_WEAK`：this paper / this work / our model / our method / 本文 / 我们 …
- `RESULT_MARKERS`：outperform / achieve / accuracy / benchmark / evaluate / experiment / state-of-the-art …（动词态，避免名词 `evaluation` 误命中 `neural network evaluations`）
- `LIMITATION_MARKERS`：limitation / fails / struggles / cannot / future work / 局限 / 不足 / 未能 …（弱词 however/but/only/still **不入**）

**渲染（`report_generator._render_paper`）**：
- 任一要素命中 → 按 `解决的问题 / 已有方案 / 新方案 / 效果及局限` 四段渲染；缺失要素标「未明确提及 / Not explicitly mentioned」。
- **四要素全空**（如摘要过短/无结构信号）→ 整段回退完整 Abstract（`condensed_abstract`，去填充 ≤1500 字符）；仍无则「暂无摘要」占位。
- 单篇块不再单列「研究内容 / 创新点 / 核心结论 / Abstract 段」（已并入四要素）。

**降级原则**：摘不到就留空/占位，**绝不编造**。

### 11.5 已知局限与未来升级 / Limitations & Future

- **规则法上限**：靠信号词猜句意，遇到「方案与结果同句」「既有方法无显式标记」「被动/倒序」等仍可能错位或留空。例如摘要里没有清晰「prior/existing method」语句时，「已有方案」只能给领域背景或留空。
- **LLM 升级（Phase 1 已实现，2026-07-13）**：`llm_analyzer.FourElementAnalyzer` 提供 **LLM 生成式 → 规则回退**分层（默认走智谱 GLM 的 Anthropic 兼容端点，复用 `ANTHROPIC_AUTH_TOKEN`，零新依赖）；`temperature=0` + 按 DOI/title 缓存。LLM 路径产出**生成式综合**（新方案编号化、效果量化、约束具名、多方案对比），达参考报告深度；失败/未配置→回退本规则版。详见 [four_element_llm_plan.md](four_element_llm_plan.md)。
- **评测**：建议在 12 场景报告上统计四要素命中率与错位率，作为规则调优 / LLM 切换的依据。

### 11.6 测试覆盖 / Test Coverage

`test/test_paper_analyzer.py::TestStructuredExtractor`：
- 四要素命中典型摘要；新方案/已有方案取不同句；
- 效果句含局限句；空摘要返回全空；
- 已有方案背景回退；既有方法(`widely used`)命中；
- 发现型贡献(`we show`)归入新方案；
- **混乱摘要不错位**（新方案↔效果不互换）回归测试。

> 单篇块四要素 + 全空回退的渲染测试见 `test/test_report_generator.py`。

---

## 12. 增量报告（定时模式）/ Incremental Report (Scheduled Mode)

> 定时/增量模式（`intent.is_scheduled=True` 或 `pipeline --incremental`）下，报告在四段式基础上加**增量标记**，且时间窗口 = `[上次报告时间, 现在]`。

### 12.1 增量标记 / Incremental Markers

- **标题下小字**（`_render_markdown`）：`*增量报告 / Incremental (since {start_date:%Y-%m-%d})*`
- **HTML title 前缀**（`_convert_to_html`）：`增量 / Incremental — {时间范围} {领域} 报告 / Report`
- **覆盖时间**（`_format_coverage_time`，既有）：自动反映增量窗口 `[last_run, now]`，如 `2026-07-06 至 2026-07-13`。
- **邮件主题**：`增量报告 / Incremental ({start} → {end})`（pipeline 透传 `send_report(subject=...)`）。

### 12.2 增量语义 / Incremental Semantics

- 论文范围：仅「上次报告时间 → 现在」之间出现的论文（`PaperSearcher` 按日期过滤 + 客户端按年兜底过滤）。
- 首次运行：无时间戳 → 用调度周期（如 weekly=7天）作基线窗口。
- 时间戳更新：**仅邮件发送成功后**写 `~/.hermes/academic_scholar_timestamps.json` 的对应 `topic_key`（`--no-email` 时生成成功即更新）。
- 空增量（0 篇新论文）：跳过报告/邮件、不更新时间戳（`--send-empty` 强制发"本期无新论文"）。

### 12.3 已知限制 / Limitations

- ~~Semantic Scholar 仅按年过滤 + `Paper` 仅存 year，客户端只能 year 级兜底~~ → **已修复（2026-07-14）**：`Paper.published_date`（日级）已由三个搜索器填充；增量客户端过滤与 `_filter_by_time` 均优先用 `published_date` 做日级比较，缺失才回退 year。S2 的「同年早段」论文不再漏入增量。
- 单进程调度（同主题勿多开，避免时间戳 JSON 读-改-写竞争）。
- 详见 [html_report_plan.md] 与 `scripts/timestamp_manager_implementation_detail.md`、`scheduler_implementation_detail.md`。

---

## 13. 已知限制与设计取舍（对照 design-init 审计） / Known Limits & Trade-offs

> 2026-07-14 对照 design-init.txt 审计后，明确以下「需求字面 vs 实现现状」的取舍，避免期望落差。已修的缺口见各模块 changelog。

| 项 | design 字面 | 实现现状 | 取舍说明 |
|---|---|---|---|
| **跨语言检索（中英混合）** | 支持中英文文献混合检索 | 中文术语自动翻译为英文后再搜索（`CN_EN_ACADEMIC` 映射表，覆盖常见 AI/ML/统计/安全等领域）；未映射的中文保留原样 | **设计原则**：搜索语言**始终英文**（`intent.language` 只控制报告呈现语言，不影响搜索）；论文标题始终为英文原文。映射表覆盖 70+ 常见术语，罕见术语建议用户直接用英文。 |
| **缺省「核心」** | 默认近 3 年中英文**核心**论文 | 默认近 3 年 + 双语 + 含预印本（核心筛选默认关） | **取舍**：arXiv 是主力源，默认剔除预印本会致结果骤减。「核心」语义实际体现在**排序优先**（顶刊/顶会/SCI·EI 加分），而非硬过滤。 |
| **文献类型筛选**（期刊/会议/学位论文） | 自动识别并按类型筛 | `paper_types` 仅解析存储，**不用于过滤** | **取舍**：arXiv 全是 preprint，强制 venue 筛选会把 arXiv 结果清空；学位论文无数据源（无 ProQuest/EThOS）。仅作信息标注。 |
| **数据源数量** | 多种数据源 arXiv 等 | 已接入 arXiv / Semantic Scholar / OpenAlex；CrossRef / PubMed 预留未接入 | 3 源满足「多种」；CrossRef/PubMed 限流已配置，Searcher 待接入。 |

### 13.1 本轮已修复的审计缺口（2026-07-14）

- ✅ **H1 latest_research**：原为死特性（解析但不应用）→ 现给近 2 年论文 +40 优先级加分（`paper_filter._priority_score(latest=True)`）。
- ✅ **H2 CrossRef/PubMed 文档**：docstring/CLAUDE/README 不再声称已实现，标注「预留」。
- ✅ **M4 时间解析**：`parse_date_range` 支持绝对日期区间（`2023-01-01至2023-12-31`）与单年（`2024年`）；月/年用 `relativedelta` 日历精确。
- ✅ **M5 APA 7th**：作者 ≤20 全列、>20 前 19+…+末位；Oxford 逗号（`A, B, & C`）。
- ✅ **M7 研究价值与应用场景**：原为孤儿字段（算而不用）→ 单篇块非空时渲染。
- ✅ **M6 增量时间精度**：`Paper` 加 `published_date`（日级，三源填充：arXiv `result.published`、OpenAlex `publication_date`、S2 `publicationDate`）；增量客户端过滤与 `_filter_by_time` 均优先日级比较、缺失回退 year → 定时增量不再把「同年早段」论文误纳入。

