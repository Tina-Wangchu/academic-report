# Academic Report Skill Requirements

# API 客户端（arXiv 检索已改为直接用 requests 查询 https API，不再依赖 arxiv 库）
scholarly==1.5.0
requests==2.31.0

# 数据处理
pandas==2.1.0
numpy==1.24.0

# 文本处理
markdown==3.5.0
jinja2==3.1.2

# 邮件
secure-smtplib==0.1.1
pysocks>=1.7.1            # 代理回退（直连失败时经 SOCKS 发邮件；缺失则仅直连）

# 工具
python-dateutil==2.8.2
pyyaml==6.0.1

# 可选：聚类分析
scikit-learn==1.3.0
